#include "main.h"

/**
 * mul - multiplies two integers
 * @a: the first arguement in the multiplication
 * @b: the second number in the multiplication
 *
 * Return: returns the result of the multiplication table
 */

int mul(int a, int b)
{
	return (a * b);
}
